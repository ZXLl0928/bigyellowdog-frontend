services:
  - type: web
    name: bigyellowdog-web
    runtime: static
    branch: main
    buildCommand: echo "static site - no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
